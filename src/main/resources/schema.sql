CREATE TABLE empleado (
                          id INT AUTO_INCREMENT PRIMARY KEY,
                          nombre VARCHAR(255),
                          edad INT,
                          direccion VARCHAR(255),
                          fechaNacimiento DATE
);